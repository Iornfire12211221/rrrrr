version: '3.8'

services:
  app:
    build: .
    ports:
      - "8081:8081"
    environment:
      - NODE_ENV=production
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped

networks:
  default:
    driver: bridge